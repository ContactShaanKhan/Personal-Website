// Each Type has its own enumeration

// Tab Type
const TabType = {
    HOME: 0,
    ABOUTME: 1,
};

const RouteType = {
    HOME: "/",
    ABOUTME: "/AboutMe",
};

// Export

export {
    TabType,
    RouteType,
};
